# Migration Script Environment Configuration

# Base URL of the Formify application
# Use http://localhost:3000 for local development
MIGRATION_BASE_URL=http://localhost:3000

# Security key for migration API endpoints
# This must match the MIGRATION_API_KEY in your app's environment
MIGRATION_KEY=secure-migration-key

# Optional: Debug mode to see more verbose output
DEBUG=false 